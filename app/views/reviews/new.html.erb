<div class="container">
  <h1>Write Reviews</h1>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.button :submit, class: 'btn btn-primary mt-3' %>
  <% end %>
</div>
